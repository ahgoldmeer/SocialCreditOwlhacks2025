# Backend Environment Template for MongoDB Integration

# Flask secret key (change in real deployment)
FLASK_SECRET_KEY=dev-secret-change-me

# MongoDB connection string (standard SRV or direct). Example for MongoDB Atlas:
# mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/trash_credit

# If you prefer to assemble the URI from discrete credentials instead of hard-coding:
# (If both discrete vars and MONGODB_URI are present, your code can choose precedence.)
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_AUTH_DB=admin

# Example of building a URI (pseudo):
# mongodb://$MONGODB_USER:$MONGODB_PASSWORD@$MONGODB_HOST:$MONGODB_PORT/$MONGODB_DB_NAME?authSource=$MONGODB_AUTH_DB

# Database name (if not included in URI path)
MONGODB_DB_NAME=trash_credit

# Optional Mongo client options
# MONGODB_MAX_POOL_SIZE=10
# MONGODB_TLS=true
# MONGODB_REPLICA_SET=rs0

# CORS origins (comma-separated). Use * ONLY for local dev.
CORS_ORIGINS=http://localhost:5173

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Host/Port for Flask app
HOST=127.0.0.1
PORT=5000
